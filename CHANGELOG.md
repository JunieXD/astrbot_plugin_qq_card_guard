# 更新记录

## 0.2.1 · 2026-09-26

- 区分 OneBot 业务失败与连接异常，兼容 aiocqhttp 的 `ActionFailed` 和失败响应字典。“消息不存在”等业务失败不再触发全局连接冷却或连接缓存失效，日志保留接口、返回码和脱敏原因。
- 无法定位旧提醒时保留人工核对状态，不推断撤回成功，不影响先行解禁；已提交但结果不明的写操作仍禁止自动重试。
- 免查缓存失效只记录一次，带旧缓存比对，防止迟到任务清除较新的核验结果。
- 日志区分操作间隔、启动缓冲、管理员恢复冷却和连接恢复。正常等待使用普通信息，连接异常和额度不足继续告警，多项等待保留各自截止时间。

## 0.2.0 · 2026-09-25

- 新增按群配置的合规/豁免免查缓存，默认60/10分钟。固定核验时间，不因发言续期；改名、角色/入群变化、规则/连接变化、临时豁免到期会失效，待处理事项继续跟进。
- 补查改为可配置分钟阶梯；禁言期间使用20～40秒的优先补查，普通成员按提醒成功时间逐档放慢。名片变化提前唤醒，不能绕过读取预算。
- 合并宽限期连发与同一轮调度的重复资料查询；关键写入前仍重新核验。写入准备过程串行读取，减少资料过期重试，取消时释放锁。
- 默认读取预算提高至600次/小时，保留分级额度；修正恢复时间计算，调整额度后重新调度旧限流任务。过时发言跳过会记录原因和计数。
- 待处理额度按群/成员去重；状态命令增加查询消耗、缓存与合并统计、暂缓和过时跳过计数，待处理列表显示下次处理时间。
- 日志记录缓存失效、免查命中、补查阶段和限流详情；修复群等级覆盖日志严重级别的问题，同一秒到达的新事件不会被旧检查完成时清除。

## 0.1.1 · 2026-09-25

- 双向处理新名片通知与缓存冲突，禁言截止时间不一致时不提交解禁；同账号再次禁言不再误认为本插件原操作。
- 提醒后收到其他禁言/解禁操作即取消本轮禁言；旧提醒撤回等待当前禁言核验完成。
- 执行时限从配置等待结束后计算，支持长随机延迟；排队或提交前删除群配置会取消处罚。
- 写入前重新确认机器人账号，校验整段资料读取的时效，排队的管理命令再次检查权限。
- 共享队列等候不再占用本地账号锁，后续解禁/撤回可参与队列；新处罚不消耗预留的高优先级读取额度。
- 修复漏收全员解禁、重新入群、角色变化通知后的持续误判；绑定账号不受其他账号离线影响。
- 待处理上限改为事务内检查；连发不再提前已排定的退避；取消任务不会覆盖已确认结果或遗留未发送意图。
- 兼容有符号消息ID，异常消息结构和复杂/空白配置按受控错误处理；修复历史排序，增加查询索引并清理48小时前的去重缓存。
- 扩充异常与并发回归测试。旧版遗留禁言若缺少新归属证据，会要求人工核对，不推断解除。

## 0.1.0 · 2026-09-25

- 每群独立正则、格式说明、示例与提醒模板，默认仅观察。
- 群身份、群等级、QQ 等级、头衔及手动名单豁免。
- 发言触发的一次性提醒、可配置禁言阶梯和个人/群/账号额度。
- 名片通知与有限补查；合规后先核验解禁，再核实并撤回原提醒。
- 持久化轮次、操作意图、间隔和未知结果暂停，管理员可私聊人工接手。
- 可配置随机等待、同账号串行、可选共享队列协调、重载/断线恢复处理。
- 完整 JSON 日志，20 MiB × 当前文件及 7 份归档，跨平台实例锁。
- Linux / Windows、Python 3.10 / 3.12 自动测试工作流。
